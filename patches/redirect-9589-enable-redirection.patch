diff --git a/redirect.module b/redirect.module
index 9f7a256..1a5df6a 100644
--- a/redirect.module
+++ b/redirect.module
@@ -557,6 +557,14 @@ function redirect_load_by_hash($hash, $reset = FALSE) {
  * @ingroup redirect_api
  */
 function redirect_load_by_source($source, $language = LANGUAGE_NONE, array $query = array()) {
+
+  // Logic changes to activate redirect for existing path starts
+  if(drupal_valid_path($source)) {
+    $request = parse_url($_SERVER['REQUEST_URI']);
+    $source = ltrim($request["path"], '/');
+  }
+  // Logic changes to activate redirect for existing path ends
+
   // Run a case-insensitive query for matching RIDs first.
   $rid_query = db_select('redirect');
   $rid_query->addField('redirect', 'rid');
